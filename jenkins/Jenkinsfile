pipeline {
    agent {
        docker {
            image 'maven:3-alpine'
            args '--security-opt seccomp=unconfined'
        }
    }
    stages {
        stage('Build') {
            steps {
                sh 'mvn -B -DskipTests'
            }
        }
        stage('Test') {
            steps {
                sh 'mvn test'
            }
            post {
                always {
                    junit 'target/surefire-reports/*.xml'
                }
            }
        }
    }
}
